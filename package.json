{
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "homepage": "https://VOPTrust.github.io/my_website"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
